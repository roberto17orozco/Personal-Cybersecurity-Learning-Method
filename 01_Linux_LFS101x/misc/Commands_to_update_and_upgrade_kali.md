# Updating and upgrading Kali Linux.

You may need to update and upgrade Kali quiet oftenly, probably once a week.

* To update: `sudo apt update`
* When update finishes, upgrade: `sudo apt full-upgrade -y`

* Or, `sudo apt update && sudo apt full-upgrade -y` for do both tasks at once.

