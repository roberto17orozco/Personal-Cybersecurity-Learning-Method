# This is only a test

This is only a test to verify I learned how to upload a new Mark Down note from terminal on Linux to the GitHub website.

Learned that a commit can be created only if the file has any changes: cannot commit if there is not a change in the file.

The purpose for the that last line is to do a new commit =).
