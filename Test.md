# This is only a test

This is only a test to verify I learned how to upload a new Mark Down note from terminal on Linux to the GitHub website.

## Commits behavior

Learned that a commit can be created only if the file has any changes: cannot commit if there is not a change in the file.

The purpose for the that last line is to do a new commit =).

## Test to confirm that commit and push are functioning on my Linux

Having a little bit of a trouble on creating a new **commit** for an existing file (This "Test.md" file)  on my Computer.

This section is only to mention that issue and to mention as well a problem on operating the **push** command.

If no other commit regarding this issues, that is because the **git push origin Main** worked corrrectly.

